const path = require('path')

module.exports = {
  idle: path.join('src', 'icons', 'idle.ico'),
  counting: path.join('src', 'icons', 'counting.ico'),
  pausing: path.join('src', 'icons', 'pausing.ico')
}
