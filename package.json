{
  "name": "pomodoro",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "electron-positioner": "^4.1.0",
    "react": "^16.8.0-alpha.0",
    "react-dom": "^16.8.0-alpha.0",
    "react-scripts": "2.1.2"
  },
  "devDependencies": {
    "cross-env": "^5.2.0",
    "electron": "^4.0.0"
  },
  "homepage": "./",
  "main": "./src/main.js",
  "scripts": {
    "serve": "react-scripts start",
    "start": "cross-env NODE_ENV=development electron .",
    "build": "react-scripts build && cross-env NODE_ENV=production npm run package",
    "static": "react-scripts build && cross-env NODE_ENV=production electron .",
    "package": "echo In development... && exit 1",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ]
}
